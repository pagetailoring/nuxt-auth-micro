const firebaseIcon = '🔥'
const userIcon = '😎'

export const updateConfig = `\n${firebaseIcon} Update utils/firebaseConfig.ts with your app's info\n\n`
export const noAuthInit = firebaseIcon + ' no Firebase Auth'

export const userChecked = userIcon + '✔️' + firebaseIcon
export const loginSuccess = userIcon + ' ✅ login...'
export const logoutSuccess = userIcon + ' ❌ logout...'
export const loginUnable = userIcon + ' Unable to log in... ' + firebaseIcon
